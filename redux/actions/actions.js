
// Settings
export const changeSettings = (settings) => ({
  type: 'CHANGE_SETTINGS',
  settings
});

export const changeCredits = (credits) => ({
  type: 'CHANGE_CREDITS',
  credits
});

// Images

export const changeActive = (image) => ({
  type: 'CHANGE_ACTIVE',
  image
});

export const changeImageList = (imageList) => ({
  type: 'CHANGE_IMAGE_LIST',
  imageList
})

// ImageStream

